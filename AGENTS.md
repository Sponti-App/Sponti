# AGENTS.md

This repository uses `CLAUDE.md` as the canonical team-vetted development guide.

Codex instructions:

- Read `CLAUDE.md` before making code changes.
- Treat `CLAUDE.md` as the source of truth for repo structure, commands, architecture, and conventions.
- For frontend work, run commands from `spa/`.
- Keep changes scoped to the current branch and task.
- Do not add private planning notes, raw meeting notes, or agent handoffs to this code repository.
- Before finishing, run the relevant checks from `CLAUDE.md` when practical and report any blockers.
